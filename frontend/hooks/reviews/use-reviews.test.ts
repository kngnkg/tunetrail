import { env } from "@/env.mjs"

describe("useReviews", () => {
  afterEach(() => {
    jest.clearAllMocks()
  })

  it("test", async () => {
    // const mockData = {
    //   // モックデータの中身を適切な形に設定する
    // }
    // const transformedMockData = {
    //   // transformReviewによって変換される想定のデータを設定する
    // }

    // global.fetch = jest.fn().mockResolvedValue({ mockData })

    // const result = await getReview(env.API_ROOT)

    // expect(result).toEqual(transformedMockData)

    console.log("test")
  })
})
